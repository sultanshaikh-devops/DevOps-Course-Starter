version: "3.3" 

services:
  testapp:
    container_name: todo-app-test
    build:      
      context: .
      dockerfile: Dockerfile
      target: test
    image: todo-app-test:latest
    volumes: 
      - type: bind
        source: .
        target: /app    